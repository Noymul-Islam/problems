#include<stdio.h>
#include<math.h>
#include<string.h>

int main()
{
    char ch[20];
     long long int a,b,i,j,m,n=0,c=0,sum;
    while(gets(ch))
    {
        m=strlen(ch);
        char ch1[m];
        for(i=m-1,j=0;i>=0;i--,j++)
           ch1[j]=ch[i];

        ch1[j]='\0';
        while(c==0)
        {
            sum=atoi(ch)+atoi(ch1);



        }



    }


}
